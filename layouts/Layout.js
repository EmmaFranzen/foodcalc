//import Nav from '../components/Nav'
//import Footer from '../components/Footer'

import Nav from "../components/Nav";

export default function Layout({ children, options, pageProps, logo }) {
    return (
        <>
            <main>{children}</main>
        </>
    )
}